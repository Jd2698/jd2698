name: Build Vue

on: 
  push:
    branches:
      - main  # o la rama de tu preferencia

jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Vue
    steps:
    - uses: actions/checkout@v2 # Paso 1: Clonar el repositorio
    - id: Build-Vue  # Paso 2: Usar la acción personalizada para desplegar la aplicación
      uses: xRealNeon/VuePagesAction@1.0.1
      with:
        username: 'Jd2698'
        reponame: 'jd2698'
        token: ${{ secrets.GITHUB_TOKEN }} # Leave this line unchanged